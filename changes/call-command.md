Fix out of sync that could happen when loading a saved game or recording where the "call" command was used extensively. (It won't fix existing recordings, but prevents the issue in new ones.)
Fix "call" command not being loaded from saved games when it's the last command done before saving.
